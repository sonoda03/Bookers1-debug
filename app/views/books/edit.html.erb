<h1>Editing Book</h1>

<%= form_with model: @book, method: :patch do  %>

<form>
<%= render 'form', book: @book %>
</form>

<p>
  <%= link_to 'Show', book_path(@book.id) %> 
  |
  <%= link_to 'Back', books_path %>
</p>
<% end %>